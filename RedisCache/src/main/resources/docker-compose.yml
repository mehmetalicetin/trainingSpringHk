version: '3.8'
services:
  db:
    image: postgres
    restart: always
    container_name: postgres-server
    environment:
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"
#  adminer:
#    image: adminer
#    restart: always
#    ports:
#      - 8080:8080